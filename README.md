# Meow Notes 3D

This project now has two entry modes:

- `index.html`: local development entry used by Vite with local `three` dependency.
- `outputs/rhythm-preview.html`: static export preview that can still be opened directly.

## Commands

```bash
npm install
npm run dev
npm run build
```
